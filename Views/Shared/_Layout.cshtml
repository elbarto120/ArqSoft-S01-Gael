<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - KIMETSU ARCHIVE</title>
    
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@400;700&family=Shojumaru&display=swap" />

    <style>
        /* Aseguramos que las variables de color funcionen en el Layout */
        :root {
            --slayer-gold: #d4af37;
            --demon-blood: #8b0000;
        }
        .navbar-brand:hover { color: white !important; }
        .nav-link:hover { color: var(--slayer-gold) !important; }
    </style>
</head>
<body style="background-color: #050505; color: white;">
    <header>
        <nav class="navbar navbar-expand-sm navbar-dark bg-black border-bottom border-warning shadow-lg mb-3">
            <div class="container-fluid">
                <a class="navbar-brand" asp-area="" asp-controller="Home" asp-action="Index" style="font-family: 'Shojumaru'; color: var(--slayer-gold);">
                    KIMETSU_DB
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse" aria-controls="navbarSupportedContent"
                        aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="navbar-collapse collapse d-sm-inline-flex justify-content-between">
                    <ul class="navbar-nav flex-grow-1">
                        <li class="nav-item">
                            <a class="nav-link text-light" asp-area="" asp-controller="Home" asp-action="Index">Inicio</a>
                        </li>
                        @* Link corregido al controlador Catalogo *@
                        <li class="nav-item">
                            <a class="nav-link text-light" asp-area="" asp-controller="Catalogo" asp-action="Index">Ver Personajes</a>
                        </li>
                        @* Link corregido para agregar nuevos personajes *@
                        <li class="nav-item">
                            <a class="nav-link text-light" asp-area="" asp-controller="Catalogo" asp-action="Agregar">Inscribir</a>
                        </li>
                    </ul>
                    <partial name="_LoginPartial" />
                </div>
            </div>
        </nav>
    </header>

    <div class="container">
        <main role="main" class="pb-3">
            @RenderBody()
        </main>
    </div>

    <footer class="footer text-muted border-top border-secondary bg-black mt-5">
        <div class="container text-center py-3">
            &copy; 2026 - <span style="color: var(--slayer-gold);">KIMETSU ARCHIVE</span> -
            <a asp-area="" asp-controller="Home" asp-action="Privacy" class="text-decoration-none text-danger">Protocolo de Privacidad</a>
        </div>
    </footer>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>